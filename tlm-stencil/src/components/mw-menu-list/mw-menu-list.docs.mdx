import { Canvas, Story, ArgsTable } from '@storybook/addon-docs';

# Menu List

The `mw-menu-list` component can be used to display menus that are not bound to an anchor element and therefore visible permanently.

<Canvas withSource="open">
    <Story id="components-menulist--default" />
</Canvas>

<ArgsTable of="mw-menu-list" />
