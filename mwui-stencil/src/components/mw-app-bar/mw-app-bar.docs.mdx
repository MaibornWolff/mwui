import { Canvas, Story, ArgTypes, Title, Subtitle } from "@storybook/addon-docs";
import * as MwAppBar from "./mw-app-bar.stories.tsx";

<Title>App Bar</Title>

<Subtitle>
  The App Bar is the main control hub of a web page and is usually placed at the top. Components can be placed into the App Bar by using the positional slots (`start`, `center`,
  `end`).
</Subtitle>

<Canvas
  of={MwAppBar.Default}
  withToolbar
  sourceState="shown"
  additionalActions={[
    {
      title: "Open in GitHub",
      onClick: () => {
        window.open("https://github.com/MaibornWolff/mwui/tree/main/mwui-stencil/src/components/mw-app-bar", "_blank");
      },
    },
  ]}
/>

<ArgTypes of={MwAppBar} />

## Examples

### Default

<Canvas of={MwAppBar.Default} />

### With Center

<Canvas of={MwAppBar.Center} />

### With Search

<Canvas of={MwAppBar.Search} />
