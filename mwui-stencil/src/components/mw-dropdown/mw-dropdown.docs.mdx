import { Canvas, Story, ArgTypes, Title, Subtitle } from "@storybook/addon-docs";
import * as MwDropdown from "./mw-dropdown.stories.ts";

<Title>Dropdown</Title>

<Subtitle>
  The dropdown component lets users pick from one of several choices by clicking on the input and selecting their choice from the menu. The options should be placed into the{" "}
  <code>dropdown-menu</code> slot as <code>mw-menu-item</code> components. Icons can be added to the component using the <code>icon-start</code> and <code>icon-end</code> slots.
</Subtitle>

<Canvas
  of={MwDropdown.Default}
  withToolbar
  sourceState="shown"
  additionalActions={[
    {
      title: "Open in GitHub",
      onClick: () => {
        window.open("https://github.com/MaibornWolff/mwui/tree/main/mwui-stencil/src/components/mw-dropdown", "_blank");
      },
    },
  ]}
/>

<ArgTypes of="mw-dropdown" />

## Examples

### Default

<Canvas of={MwDropdown.Default} />

### Horizontal

<Canvas of={MwDropdown.Horizontal} />

### With Error

<Canvas of={MwDropdown.WithError} />
