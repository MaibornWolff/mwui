import { Canvas, Story, ArgsTable } from "@storybook/addon-docs";

# Slider

The `mw-slider` component is a wrapper around the native range input element. It additionally provides optional label, helper-text and icons at the start or end.

<Canvas>
  <Story id="components-slider--default" />
</Canvas>

<ArgsTable of="mw-slider" />
