import { Canvas, Story, ArgTypes, Title, Subtitle } from "@storybook/addon-docs";
import * as MwAutocompleteStories from "./mw-autocomplete.stories.tsx";

<Title>Autocomplete</Title>

<Subtitle>
  The autocomplete allows the user to pick from a predetermined list of options, or enter their own text. The component allows either one or multiple entries if the{" "}
  <code>multiple</code> prop is set to true. The dropdown options should be placed in the <code>dropdown-menu</code> slot. Additionally an icon can be added to the start of the
  input box using the <code>icon-start</code> slot.
</Subtitle>

<Canvas
  of={MwAutocompleteStories.Multiple}
  withToolbar
  sourceState="shown"
  additionalActions={[
    {
      title: "Open in GitHub",
      onClick: () => {
        window.open("https://github.com/MaibornWolff/mwui/tree/main/mwui-stencil/src/components/mw-autocomplete", "_blank");
      },
    },
  ]}
/>

<ArgTypes of="mw-autocomplete" />

## Examples

## Default

<Canvas of={MwAutocompleteStories.Default} />

## Multiple

<Canvas of={MwAutocompleteStories.Multiple} />

## Inline

<Canvas of={MwAutocompleteStories.Inline} />

## Disabled

<Canvas of={MwAutocompleteStories.Disabled} />
