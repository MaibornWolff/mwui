import { Canvas, Story, ArgTypes, Title, Subtitle } from "@storybook/addon-docs";
import * as MwChipInput from "./mw-chip-input.stories.tsx";

<Title>Chip Input</Title>

<Subtitle>The Chip Input component allows users to enter multiple entries into an input field.</Subtitle>

Icons can be added to the input field using the `icon-start` and `icon-end` slots.

<Canvas
  of={MwChipInput.Default}
  withToolbar
  sourceState="shown"
  additionalActions={[
    {
      title: "Open in GitHub",
      onClick: () => {
        window.open("https://github.com/MaibornWolff/mwui/tree/main/mwui-stencil/src/components/mw-chip", "_blank");
      },
    },
  ]}
/>

<ArgTypes of="mw-chip-input" />

## Examples

## Default

<Canvas of={MwChipInput.Default} />

## Horizontal

<Canvas of={MwChipInput.Horizontal} />

## Disabled

<Canvas of={MwChipInput.Disabled} />

## Error

<Canvas of={MwChipInput.Error} />
